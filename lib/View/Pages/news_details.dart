import 'package:flutter/material.dart';

class NesDetails extends StatelessWidget {
  const NesDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
